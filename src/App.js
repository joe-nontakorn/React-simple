import React from 'react';
import CustomRouter from './router/router'; // Adjust the path if needed

const App = () => {
  return (
    <React.StrictMode>
      <CustomRouter />
    </React.StrictMode>
  );
};

export default App;